{
	"name": "qhoami",
	"displayName": "qhoami",
	"description": "Session identity + reboot counting for VS Code agents — standalone Node library + extension",
	"version": "0.8.0",
	"publisher": "vgm9",
	"engines": {
		"vscode": "^1.109.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onStartupFinished"
	],
	"main": "./out/extension.js",
	"exports": {
		".": {
			"types": "./out/lib.d.ts",
			"default": "./out/lib.js"
		}
	},
	"types": "./out/lib.d.ts",
	"contributes": {
		"commands": [
			{
				"command": "qhoami.showIdentity",
				"title": "qhoami: Show Identity JSON",
				"icon": "$(account)"
			}
		],
		"languageModelTools": [
			{
				"name": "qhoami",
				"displayName": "Get Session ID",
				"canBeReferencedInPrompt": true,
				"toolReferenceName": "qhoami",
				"icon": "$(account)",
				"userDescription": "Get your current chat session ID",
				"modelDescription": "Returns the definitive current chat session ID from toolInvocationToken. No parameters needed. This is the ONLY reliable way to get YOUR session ID.",
				"inputSchema": {
					"type": "object",
					"properties": {},
					"required": []
				}
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"package": "npx vsce package --allow-missing-repository -o qhoami.vsix",
		"cli": "node out/cli.js"
	},
	"devDependencies": {
		"@types/node": "^22.10.5",
		"@types/vscode": "^1.109.0",
		"@vscode/vsce": "^3.7.1",
		"typescript": "^5.9.3"
	},
	"__metadata": {
		"installedTimestamp": 1770954935059,
		"targetPlatform": "undefined",
		"size": 11869
	}
}
